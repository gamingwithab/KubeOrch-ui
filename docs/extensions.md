# Extensions catalog

The UI formerly labelled its CRD authoring catalog **CRD Plugins**. User-facing navigation and catalog copy now use **Extensions**.

The existing `/dashboard/integrations/plugins` route and internal `Plugin*` TypeScript, store, and service identifiers remain unchanged for compatibility. This terminology change does not alter enable/disable behavior or claim installation, health, or operational compatibility.
